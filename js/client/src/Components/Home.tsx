import * as React from 'react';
import { Box, Toolbar, Typography } from '@mui/material';

export const Home = <Box component="main" sx={{ flexGrow: 1, p: 3 }}>
  <Toolbar />
  <Typography paragraph>
    Welcome to NLIO
  </Typography>
</Box>;
